# Investor documents — update these whenever the Google Drive links change.
NEXT_PUBLIC_INVESTOR_PITCH_DECK_URL=https://drive.google.com/file/d/YOUR_PITCH_DECK_FILE_ID/view?usp=sharing
NEXT_PUBLIC_INVESTOR_FINANCIAL_MODEL_URL=https://docs.google.com/spreadsheets/d/YOUR_FINANCIAL_MODEL_FILE_ID/edit?usp=sharing

# Resend API key — server-side only, never expose
# Get yours at https://resend.com → API Keys
RESEND_API_KEY=re_SSSAsUVB_F1XWD312G6WGfPuMAriL3HbH

# Optional: sender address. Must be on a verified domain.
# If unset, defaults to "AequorOS <onboarding@resend.dev>" (Resend's shared testing domain).
# Before launch, verify aequoros.com in Resend (Settings → Domains) and set this to e.g.
# RESEND_FROM_EMAIL="AequorOS <noreply@aequoros.com>"
RESEND_FROM_EMAIL=noreply@aequoros.com

# Optional: destination address for contact inquiries. Defaults to eric@aequoros.com.
RESEND_TO_EMAIL=eric@aequoros.com
