/**
 * risk-service
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 0.1.0
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import * as runtime from "../runtime";
import type { CashflowWindowRead, ErrorResponse } from "../models/index";
import {
  CashflowWindowReadFromJSON,
  CashflowWindowReadToJSON,
  ErrorResponseFromJSON,
  ErrorResponseToJSON,
} from "../models/index";

export interface ComputeCashflowWindowRequest {
  bankId: string;
  startDate: Date;
  endDate: Date;
}

/**
 *
 */
export class CashflowWindowApi extends runtime.BaseAPI {
  /**
   * Contractual cash flows maturing inside [start_date, end_date]: the current canonical book\'s per-currency, per-calendar-month inflows, outflows and nets, with per-currency and base-equivalent totals plus the count of positions carrying no contractual maturity (excluded).
   * Compute Cashflow Window
   */
  async computeCashflowWindowRaw(
    requestParameters: ComputeCashflowWindowRequest,
    initOverrides?: RequestInit | runtime.InitOverrideFunction,
  ): Promise<runtime.ApiResponse<CashflowWindowRead>> {
    if (requestParameters["bankId"] == null) {
      throw new runtime.RequiredError(
        "bankId",
        'Required parameter "bankId" was null or undefined when calling computeCashflowWindow().',
      );
    }

    if (requestParameters["startDate"] == null) {
      throw new runtime.RequiredError(
        "startDate",
        'Required parameter "startDate" was null or undefined when calling computeCashflowWindow().',
      );
    }

    if (requestParameters["endDate"] == null) {
      throw new runtime.RequiredError(
        "endDate",
        'Required parameter "endDate" was null or undefined when calling computeCashflowWindow().',
      );
    }

    const queryParameters: any = {};

    if (requestParameters["startDate"] != null) {
      queryParameters["start_date"] = (requestParameters["startDate"] as any)
        .toISOString()
        .substring(0, 10);
    }

    if (requestParameters["endDate"] != null) {
      queryParameters["end_date"] = (requestParameters["endDate"] as any)
        .toISOString()
        .substring(0, 10);
    }

    const headerParameters: runtime.HTTPHeaders = {};

    if (this.configuration && this.configuration.accessToken) {
      const token = this.configuration.accessToken;
      const tokenString = await token("HTTPBearer", []);

      if (tokenString) {
        headerParameters["Authorization"] = `Bearer ${tokenString}`;
      }
    }
    const response = await this.request(
      {
        path: `/api/v1/banks/{bank_id}/analytics/cashflow-window`.replace(
          `{${"bank_id"}}`,
          encodeURIComponent(String(requestParameters["bankId"])),
        ),
        method: "GET",
        headers: headerParameters,
        query: queryParameters,
      },
      initOverrides,
    );

    return new runtime.JSONApiResponse(response, (jsonValue) =>
      CashflowWindowReadFromJSON(jsonValue),
    );
  }

  /**
   * Contractual cash flows maturing inside [start_date, end_date]: the current canonical book\'s per-currency, per-calendar-month inflows, outflows and nets, with per-currency and base-equivalent totals plus the count of positions carrying no contractual maturity (excluded).
   * Compute Cashflow Window
   */
  async computeCashflowWindow(
    requestParameters: ComputeCashflowWindowRequest,
    initOverrides?: RequestInit | runtime.InitOverrideFunction,
  ): Promise<CashflowWindowRead> {
    const response = await this.computeCashflowWindowRaw(
      requestParameters,
      initOverrides,
    );
    return await response.value();
  }
}
